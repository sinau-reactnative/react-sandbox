import React, { Fragment } from "react";

import Header from "../components/Header";

const Setting = () => {
  return (
    <Fragment>
      <Header />
      <h1>Setting</h1>
    </Fragment>
  );
};

export default Setting;
